{% if group and group.gallery %}
  <section title="gallery" class="gallery">
    {% for picture in group.gallery %}
      <picture>
        <img src="{{ picture.image }}" alt="{{ picture.alt }}" loading="lazy">
      </picture>
    {% endfor %}
  </section>
{% endif %}
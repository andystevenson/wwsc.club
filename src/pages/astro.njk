{% set group = astro %}

<h1>{{ title | capitalize }}</h1>
<p class="description">{{ astro.description }}</p>

{% set promotions = cms.tags['astro-promotion'] %}
{% include "partials/promotions.njk" %}

{% include 'partials/gallery.njk' %}

{{ linkc(cms.links['email-football'], h2('book the astro'), '?Subject=Book the Astro', class='booking') }}
